[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "localitas-client"
version = "0.1.0"
description = "Python SDK for the Localitas platform API"
requires-python = ">=3.14"
dependencies = ["websocket-client>=1.6.0"]
